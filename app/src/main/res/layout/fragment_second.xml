<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

    </data>

    <androidx.core.widget.NestedScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".SecondFragment"
        android:fillViewport="true">

        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:padding="16dp">

            <Button
                android:id="@+id/buttonStartHere"
                android:layout_width="226dp"
                android:layout_height="110dp"
                android:layout_margin="15dp"
                android:backgroundTint="@color/md_theme_light_tertiary"
                android:fontFamily="@font/lato"
                android:text="@string/start_here"
                android:textSize="24sp"
                app:cornerRadius="@dimen/fab_margin"
                app:layout_constraintBottom_toTopOf="@+id/buttonLearningModules"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintVertical_bias="0.765" />

            <Button
                android:id="@+id/buttonLearningModules"
                android:layout_width="226dp"
                android:layout_height="110dp"
                android:layout_marginBottom="236dp"
                android:backgroundTint="@color/md_theme_light_tertiary"
                android:fontFamily="@font/lato"
                android:text="@string/learning_modules"
                android:textSize="24sp"
                app:cornerRadius="@dimen/fab_margin"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintStart_toStartOf="parent" />

        </androidx.constraintlayout.widget.ConstraintLayout>
    </androidx.core.widget.NestedScrollView>
</layout>